# MolamIA One Page

Landing page Next.js one-page per MolamIA.

## Render
Build Command: `npm install && npm run build`
Start Command: `npm start`
Root Directory: lascia vuoto se questi file sono alla root del repository.
